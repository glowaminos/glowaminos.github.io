import {PRODUCTS} from './products.js';import {priceHtml,initials} from './components.js';import {addToCart} from './cart.js';
const p=PRODUCTS.find(x=>x.slug===document.body.dataset.productSlug),r=document.querySelector('[data-product-detail]');
if(!p){r.innerHTML='<div class="empty-state"><h1>Product not found</h1></div>'}else{r.innerHTML=`<div class="product-visual"><small>${p.category}</small><div class="code">${initials(p.name)}</div><small>Research use only</small></div><div class="product-info"><div class="eyebrow">${p.category}</div><h1 class="title">${p.name}</h1>${priceHtml(p)}<p class="lead">Only verified catalog details are displayed. Unverified variants, strengths, purity, lot numbers, stock claims and scientific specifications are not invented.</p><div class="product-buy"><div class="qty"><button data-dec>−</button><input data-qty value="1" inputmode="numeric"><button data-inc>+</button></div><button class="btn" data-add>Add to cart</button></div><div class="notice">For laboratory research use only. Not for human or veterinary use.</div><div class="tabs"><section class="tab-section"><h2>Verified catalog details</h2><p class="muted">Product name and displayed pricing come from the verified source material. Where the source shows “From”, the starting price is preserved without inventing variants.</p></section><section class="tab-section"><h2>Specifications</h2><p class="muted">No additional specifications are shown because they were not verifiable from the source material available during this build.</p></section><section class="tab-section"><h2>Quality documentation</h2><p class="muted">Merchant-specific COAs, lot numbers and laboratory claims must be connected before publication.</p></section></div></div>`;const q=r.querySelector('[data-qty]');r.querySelector('[data-inc]').onclick=()=>q.value=Math.min(99,(parseInt(q.value)||1)+1);r.querySelector('[data-dec]').onclick=()=>q.value=Math.max(1,(parseInt(q.value)||1)-1);r.querySelector('[data-add]').onclick=()=>addToCart(p.id,Math.max(1,parseInt(q.value)||1))}
