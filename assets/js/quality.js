const target=document.querySelector('[data-quality-stages]');fetch('data/testing.json').then(r=>{if(!r.ok)throw Error();return r.json()}).then(rows=>{target.innerHTML=Array.isArray(rows)&&rows.length?rows.map((stage,i)=>`<div class="testing-row"><span>${String(i+1).padStart(2,'0')}</span><strong>${stage.name}</strong><small>${stage.detail||''}</small></div>`).join(''):'<p>Testing stages will be listed here when methods and records are supplied. No purity or testing result is claimed without documentation.</p>'}).catch(()=>target.textContent='Documentation is unavailable. Refresh to try again.');
