import {PRODUCTS} from './products.js';import {productCard,getPrice} from './components.js';
const g=document.querySelector('[data-shop-grid]'),c=document.querySelector('[data-product-count]'),s=document.querySelector('[data-search]'),o=document.querySelector('[data-sort]');let cat='All';const requestedCategory=new URLSearchParams(location.search).get('category');if(requestedCategory&&document.querySelectorAll('[data-filter]').length){const match=[...document.querySelectorAll('[data-filter]')].find(b=>b.dataset.filter===requestedCategory);if(match){cat=requestedCategory;document.querySelectorAll('[data-filter]').forEach(b=>b.classList.toggle('active',b===match))}}
function render(){let a=[...PRODUCTS],q=(s.value||'').trim().toLowerCase();if(q)a=a.filter(p=>(p.name+' '+p.category).toLowerCase().includes(q));if(cat!=='All')a=a.filter(p=>p.category===cat);if(o.value==='name-az')a.sort((x,y)=>x.name.localeCompare(y.name));else if(o.value==='name-za')a.sort((x,y)=>y.name.localeCompare(x.name));else if(o.value==='price-low')a.sort((x,y)=>getPrice(x)-getPrice(y));else if(o.value==='price-high')a.sort((x,y)=>getPrice(y)-getPrice(x));else a.sort((x,y)=>Number(y.featured)-Number(x.featured));g.innerHTML=a.length?a.map(productCard).join(''):'<div class="empty-state" style="grid-column:1/-1">No matching products.</div>';c.textContent=`${a.length} product${a.length===1?'':'s'}`}
let t;s.oninput=()=>{clearTimeout(t);t=setTimeout(render,120)};o.onchange=render;document.querySelectorAll('[data-filter]').forEach(b=>b.onclick=()=>{cat=b.dataset.filter;document.querySelectorAll('[data-filter]').forEach(x=>x.classList.toggle('active',x===b));render()});render();
