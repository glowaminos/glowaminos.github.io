import {SITE_CONFIG} from './config.js';
async function send(form,endpoint,status){if(!endpoint){status.className='form-status error';status.textContent='This form is not connected yet. Add a real endpoint in assets/js/config.js.';return}status.textContent='Sending…';try{const r=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(form)))});if(!r.ok)throw new Error('Request failed.');status.textContent='Submitted successfully.';form.reset()}catch(e){status.className='form-status error';status.textContent=e.message}}
const c=document.querySelector('[data-contact-form]');if(c)c.onsubmit=e=>{e.preventDefault();send(c,SITE_CONFIG.contactEndpoint,document.querySelector('[data-contact-status]'))};const n=document.querySelector('[data-newsletter-form]');if(n)n.onsubmit=e=>{e.preventDefault();send(n,SITE_CONFIG.newsletterEndpoint,document.querySelector('[data-newsletter-status]'))};
